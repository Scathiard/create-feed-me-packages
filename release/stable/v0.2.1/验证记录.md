# 验证记录：0.2.1 正式版

- 日期：2026-09-16
- 分支：**`release/v0.2.1`**（构建基线提交见下）
- 基线：已发布 **0.2.0**（tag `v0.2.0` = 提交 `3670fad`）

## 一、源码＝用户验收过的 `.62` 源码（原始命令输出）

| 命令 | 输出 |
| --- | --- |
| `git diff 3e27aeb -- src` | **空**（⇒ 本版 `src/` 与用户实机验收的 `0.2.1-test.62` 源码逐字相同） |
| `git diff fe42c7e 3670fad -- src` | **空**（⇒ 已发布 v0.2.0 与其上游 `fe42c7e` 的 `src/` 也逐字相同，两套基线一致） |

**构造方式（与"从 `3670fad` 起分支再 `checkout 3e27aeb -- src`"等价，但零删除）**：本分支从 `3e27aeb`（用户验收的源码提交）起，再把 v0.2.0 的 `CHANGELOG.md` 与 `release/v0.2.0/**` 取进来（`git checkout 3670fad -- CHANGELOG.md release/v0.2.0`）。
**为什么换构造**：`git checkout <commit> -- src` **不会删除**"该提交里没有、工作区里却有"的文件——而 0.2.0 里的 `src/main/java/dev/scathiard/feedmepackages/mixin/CraftingRefillMixin.java` 正是本版要移除的那个（实测按原配方做完后 `git diff 3e27aeb -- src` 非空、只有这一个文件）。**删文件属红线**，故改用"从目标源码起、把 v0.2.0 的文档取回来"的零删除构造；两种构造的最终树都是"`src/` = `3e27aeb` ＋ 树里有 `CHANGELOG.md`"。

## 二、产物（按 SHA-256 认）

| 项 | 值 |
| --- | --- |
| jar（**包目录内**） | `release/v0.2.1/create-feed-me-packages-0.2.1.jar`（**与本目录文件表一致**；构建产物同时在共享构建目录 `D:\claude_sandbox\.fmp-build-safe\libs\create-feed-me-packages-0.2.1.jar`，两处逐字节相同） |
| 字节数 | **366,915** |
| SHA-256 | **`93622b3f4125ebffe2018c287606994519784f46ae9016c0aae203088d708cf7`** |
| 包内 `META-INF/neoforge.mods.toml` | `version="0.2.1"`（解包实读） |
| `gradle.properties` | `mod_version=0.2.1` |
| 源码快照 | `release/v0.2.1/v0.2.1-source.zip`（**在本包目录内**；`git archive` 本分支 HEAD；哈希见 `SOURCE_SHA256SUMS.txt`） |

> 共享 `.fmp-build-safe` 是两棵树共用的构建目录；**一律按 SHA-256 认产物**。本版 jar 名与 `.62`／0.3 各版都不同名，不会互相覆盖。

## 三、条目差集（原始输出）

**(a) 与用户验收过的 `.62` jar（`08631ed4…`／366,929 B）比**：
- 条目数 **212 / 212**，**ADDED = `[]`**、**REMOVED = `[]`**；
- **内容不同的条目只有 2 个**：`META-INF/MANIFEST.MF`、`META-INF/neoforge.mods.toml` —— **都只差版本字符串**（`0.2.1-test.62` → `0.2.1`）。⇒ **行为与用户验收过的那一版逐字相同，只差版本号**。

**(b) 与已发布 0.2.0 jar（`2d5cf947…`／365,531 B，实测复核过）比**：
- 条目数 **212 / 214**，**ADDED = `[]`**；
- **REMOVED 恰为两条**：`dev/scathiard/feedmepackages/consumption/CraftingService$Refill.class`、`dev/scathiard/feedmepackages/mixin/CraftingRefillMixin.class`（＝"取料续填移除"整条路径）。

## 四、三条合同命令（实跑）

| 命令 | 结果 |
| --- | --- |
| `.\gradlew.bat test build --offline --no-daemon --no-configuration-cache --console=plain` | **BUILD SUCCESSFUL**；JUnit **25/25**（3 套件、0 失败；结果 XML 与日志随本目录） |
| `.\gradlew.bat runGameTestServer -PfmpGameTests -PwithMobile --offline --no-daemon --no-configuration-cache --console=plain` | **155 GAME TESTS COMPLETE / All 155 required tests passed**（22:18:16） |
| `.\gradlew.bat runServer -PfmpPackaged -PwithMobile --offline --no-daemon --no-configuration-cache --console=plain` | 成品冒烟通过：`FMP_PACKAGED_SOURCE jar=…create-feed-me-packages-0.2.1.jar developmentOutputs=0`；Mod 列表 **`Create: Feed Me Packages! 0.2.1`**；`Loading Create: Feed Me Packages! 0.2.1`；**`Done (1.934s)!`**；**无异常栈**。 |

日志：`t69-test-build.log`、`t69-gametest.log`、`t69-packaged-server-smoke.log`。

## 五、冻结物零改动（复核）

| 对象 | 复核值 |
| --- | --- |
| `release/testing/v0.2.1-test.62/…jar` | **366,929 B／`08631ed4dd8d96bc511729cf1377c5858080443370a8b256aa23ea644ad4999c`**（一字节未动） |
| `release/v0.2.0/**`、`release/v0.1.0/**` | 在本分支里来自 `3670fad`／`3e27aeb` 的**原样文件**，未编辑 |
| 主仓 `D:\claude_sandbox\Create-Feed Me Packages!` | 工作区未被写入（`git status` 无已跟踪改动） |

## 六、未覆盖与已知限度（如实）

1. **没有在真实客户端点过 JEI「+」**：本版客户端半段只有代码＋埋点日志证据；实机验法见《测试清单.md》三步。
2. **重度整合包里 JEI「+」可能对工作台配方一直是灰的、处理器不会被询问**（原因尚未定论；已排除 JEI 版本差异与注册覆盖）。**原版配方书与面板取物不受影响**，本版主修复（不再重复消耗）不依赖 JEI 路径。
3. 成品冒烟里有一条**历史性版本警告**：`create_feed_me_packages (version 0.2.1-test.62 -> 0.2.1)`——来自共享运行档位里残留的旧依赖声明（不是本包声明；本包 `neoforge.mods.toml` 只声明 neoforge/minecraft/create/curios），**不是加载失败**（服务器仍 `Done`），原样记录。
4. 未推送、未建 tag、未发 Release（由队长执行）。
