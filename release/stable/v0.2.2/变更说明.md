# 变更说明（0.2.2）

## 这一版是什么

0.2.2 是 0.2 线的兼容/体验修正版本，正式口径只有两件事：

1. **JEI 接入只走官方 API**（`IRecipeTransferRegistration`）：本模组**不使用任何针对 JEI 的 mixin／反射**，也**不替换、不否决、不包裹**任何第三方已注册的处理器。
2. **结果看得见**：JEI 发起的转移，其裁定改用**动作栏**说出（失败必说并尽量点名缺什么；成功静默）；悬停预览按 **v3「按钮不许撒谎」**（能点＝能成；能证明不能做就灰并点名缺什么；说不清才放行）。

## 重大更正：0.2.2 曾采用"接管"方案，已被用户禁止，本包已撤除

- 曾有一版 0.2.2 用 **client-only mixin** 拦截 `mezz.jei.library.recipes.RecipeTransferManager#getRecipeTransferHandler`，在"面板活着"时**替换**该位置原本的处理器（第三方注册在同一个 `(CraftingMenu, minecraft:crafting)` 键上，而 JEI 的注册表是后写覆盖）。用户裁定：**「所以这是直接劫持了 jei 原本的 + ？禁止这种做法」**——**成立，禁止**。
- **本包已撤除**：删除 `compat/jei/takeover/TakeoverRecipeTransferManagerMixin` 与 `fmp_jei_takeover.mixins.json`，并移除 `neoforge.mods.toml` 中指向它的那个 `[[mixins]]`；`FmpJeiPlugin`／`FmpRecipeTransfer` 里"记住被顶掉的处理器并先委托它"的那条路一并删除；`nativeTransfer` 回到**只用** JEI 官方给"未注册处理器"用的通用兜底（纯客户端模拟，不碰他人注册）。
- **静态铁证**：本包**只有一个** mixin 配置 `create_feed_me_packages.mixins.json`（`package=dev.scathiard.feedmepackages.mixin`、`plugin=…mixin.OptionalMixins`、全文不含 `mezz.jei`）；jar 内**没有任何** `takeover`／`fmp_jei_takeover` 条目。这条红线另有单测 `JeiIntegrationIsApiOnlyTest`（3 例）常驻守卫。

## 已知问题（与 v0.2.1 同口径：本版不修，也不再尝试修）

- **装了「机械动力：存储」（Create: Storage，`fxntstorage`）时**，JEI 取料合成点击「+」不会使用缓存里的物品。原因已查清：该模组会**无条件抢占**"工作台配方转移"这一个注册位（JEI 同一位置只允许一个处理器、后注册者胜），而它只认身上背的背包，于是工作台配方的「+」呈**灰**并给出**原版「缺少物品」**提示。**与本模组无关，也不只影响本模组**——任何在它之后注册该位置的模组同样不生效。
- **代价（撤除接管后必须说清）**：在装有该第三方模组的包里，JEI 的「+」**由它回答，不是我们** ⇒ **缓存补料不走「+」**。需要缓存供料时请用**我们自己的物流面板**或**原版配方书**（材料在缓存里照常供料）。
- 若 JEI 客户端界面本身异常（与本模组无关的情况），上述两条供料路径照旧可用。

## 结果看得见（本版保留）

JEI 发起的转移过去是**静默失败**：意图发出即当成功（`FILL_RECIPE` 只回一个 bool），而服务端的裁定只画在**面板的 notice** 上——可 JEI 配方页正盖着面板，**那句话玩家根本看不见**（面板只在它自己的容器界面渲染；本版核实过渲染门控）。现在：

- **失败**：用**动作栏**（`player.displayClientMessage(…, true)`，任何界面都可见）说出**我们自己的**原因文案（`gui.create_feed_me_packages.result.*`），并**尽量点名缺什么**——`缺：<物品> ×N, …`；
- **成功**：**不额外刷屏**（材料自己会出现）；面板里的 notice **保持原样**、未改；
- **口径**：那份"缺什么"由**客户端估算**给出，并**无条件**注明它只算了哪些料源（"只算了合成格、随身格与缓存"）。它**只能**作为"能点名 ⇒ 可以灰"的依据，**不能**反过来把"点名不出来"当成拒绝理由；真正的裁决仍归服务端；
- 服务端的失败裁定走同一条动作栏，**确认超时**也会说一句（不再是沉默）。

## 料源与预览 v3（本版保留）

- **点击「+」**：先跑 **JEI 自己的转移**（`helper.createUnregisteredRecipeTransferHandler` 的通用兜底，纯客户端模拟），再让服务端按剩余缺口从**缓存**补齐（既有的 `FILL_RECIPE`）；**服务端裁决语义不变**。每次点击留**一行** INFO 汇总（`preview=`／`native=`／`topup=`／`notice=`／`missing=[…]`）。
- **悬停预览 v3**（能点＝能成；成不了＝灰 ＋ 说清缺什么；按顺序第一条命中即返回）：
  1. JEI 自己的通用转移能成 ⇒ **放行**；
  2. 无客户端材料视图 ⇒ **放行**（交回 JEI 自己的路径）；
  3. 否则看**我们自己的估算**（与服务端同源规划器：合成格＋随身格＋缓存视图）：`OK` ⇒ 放行；`MISSING` **且能点名** ⇒ **灰**并写出 `缺：…`（附"只算了合成格、随身格与缓存"）；`MISSING` **但点名不出来** ⇒ **放行**（证明不了就不许拒绝，唯一安全阀）；`NO_SPACE`／`UNSUPPORTED`／`TOO_COMPLEX` ⇒ **灰**并给对应既有文案；`INACTIVE` ⇒ 放行。
  - 预览每条链路留**一行** INFO，字段含 `decision=allow-native|allow-estimate-ok|allow-unprovable|refuse:<reason>` 与 `missing=[…]`。

## 影响面与兼容

- **协议不变**、**存档 schema 不变**（仍 8）、**服务端裁决语义不变**。
- **干净包（没有第三方抢该位置）**：我们的处理器就是 JEI 那个位置的所有者，行为与 0.2.1 一致；0.2.2 多出来的只是**动作栏交代**与**预览 v3**。
- **面板不在时**（未佩戴坠子／面板未打开）：走 **JEI 自己的转移**（背包照常可用），日志说明未查缓存 ⇒ 两个功能都不丢。

## 同轮第二刀：撤除"帮 JEI 办事"的整层（含登记表与文案层）

用户裁定原话：**「这当然也是被勿增的实体，jei 在缺料的时候可不会弹出来一条文字让你阅读，而是直接标红缺少的材料。我的意思是，我们就不该有这一层"帮 jei 把事办了"的层」**。判据落地：JEI **自己**把缺的材料标红、**自己**决定「+」能不能点；我们**不该**在旁边替它裁决，更不该替它写作文。本模组定位收缩为：**我们有我们自己的取料入口（面板 ＋ 原版配方书），JEI 保持原样**。

**这一刀删掉的**：
- **删文件**：`compat/jei/FmpRecipeTransfer.java`、`compat/jei/PreviewPolicy.java`、`client/ClientNotice.java`、`client/NoticeText.java`；对应单测 `PreviewPolicyTest.java`／`ProvablyUnsolvableTest.java`／`ClientNoticeTest.java`。
- **`FmpJeiPlugin`**：删 `registerRecipeTransferHandlers(...)` 整个方法、`craftingHandler` 字段与 `craftingHandler()` 访问器、以及那行 "FMP JEI transfer handlers registered" 日志。**保留** `onRuntimeAvailable/onRuntimeUnavailable`（我们面板的会话与排版）、`registerVanillaCategoryExtensions`（让 JEI **显示**我们的坠子锻造配方，显示≠代办）、`registerGuiHandlers`（我们面板的排除区）。
- **`LogisticsPanel`**：删 `fillRecipeForViewer`／`speakViewerResult`／`speakViewerTimeout` 与 `viewerWaiting`／`viewerMissing`；`tick()` 的超时**回到原本那一行** `notice("timeout")`；**`fillRecipe` 与 `notice` 原样保留**（面板补料与面板提示是我们**自己的门**）。
- **`ClientCrafting`**：为那一层长出来的 `Estimate`／`estimate(...)`／`shortfall(...)` **收回**成单一 `check(...)`，语义与判定顺序**逐字不变**（仍先 `MISSING` 后 `NO_SPACE`）；唯一调用者仍是原版配方书的 `RecipeCollectionHintsMixin`。
- **lang**：删只服务那一层的 3 个键（`result.missing_entry`／`result.missing_detail`／`result.missing_scope`，中英各一份；删后两份 JSON 仍可解析、无 BOM）；**`result.*` 其余键保留**（面板 `notice("result." + …)` 仍在用）。
- **`JeiClientReview`**（客户端复核用例）：原来断言"JEI 返回的处理器必须是我们的"，现在改成断言**不得出现属于本模组的转移处理器**。

**代价（写清楚）**：这一层删掉后，**任何整合包里本模组都不再让 JEI 的「+」吃缓存**——JEI 的「+」由 JEI 自己（或该注册位上的第三方）回答；缓存取料走**我们自己的物流面板**与**原版配方书**。

**仍然保留（判据一：功能不许被剃掉）**：① **物流面板**（缓存取料＋面板补料）；② **原版配方书**（`RecipeCollectionHintsMixin`／`RecipeBookHintsMixin`／`RecipeBookPlacementMixin`＋服务端 `CraftingService.fromRecipeBook`：配方书显示"可合成"且点击**真能从缓存取料**，显示与能力一致）；③ `MaterialHints`／`ClientMaterials`（缓存视图）；④ JEI 插件里"面板会话／排除区／锻造配方显示"三件。

**守卫**：单测 `JeiIntegrationIsApiOnlyTest`（5 例）扩到断言这一层不得回来——jar/类路径里不得有那四个类，`FmpJeiPlugin` 的字节码里不得出现 `registerRecipeTransferHandlers`／`craftingHandler`／`mezz/jei/api/recipe/transfer`，两个 lang 文件不得再有那三个键，同时断言"面板会话（`recipeOverlay`）／锻造显示／排除区"仍在。
## 追加（同轮第三件）：与 Create: Storage 的兼容注入（软、可选、可退化）

**要解决的问题**：装了 `fxntstorage`（Create: Storage）的包里，JEI 工作台的「+」归**他们**（他们后注册、拿走 `(CraftingMenu, CRAFTING)` 唯一槽位），我们的缓存**没法通过「+」取料**。**用户裁定**：**不得接管/替换他人已注册行为**（红线），**目标是与他们的兼容**，且为实现功能而增的实体是必要的。

**机制（只做一件事：把我们的缓存当成他们的第三个货源）**

- **两个定点注入**（独立软配置 `fmp_fxntstorage_compat.mixins.json`：`required:false` ＋ `defaultRequire:0`，三个 `@Pseudo` mixin）——**他们自己的** JEI 客户端预览与**他们自己的**服务端传输路径里，那一次"取背包处理器"的**局部变量**被换成一个**只在那一次调用里存在**的包装器：
  - 客户端：`net.fxnt.fxntstorage.compat.jei.JEICraftingTransferHandler.transferRecipe`（1.3.x 与 1.1.x 形状相同）；
  - 服务端：1.3.x 的 `net.fxnt.fxntstorage.network.packet.TransferRecipePacket.handle`（两处 store：随身背包 / 打开的背包菜单）与 1.1.x 的 `net.fxnt.fxntstorage.network.handler.ServerPayloadHandler`（`method="*"` 以覆盖编译器生成的 lambda 体）。
- **包装器**继承 `net.neoforged.neoforge.items.ItemStackHandler`（他们那两个调用点的声明类型就是它，必须是子类）：
  - 他们的格子**非空** ⇒ **原样返回他们的**（我们永不改动他们的真实物品，写回也走 `super`）；
  - 他们的**空物品槽**（限定在 `BackpackSlotLayout.items()` 范围内；1.1.x 用 `Util.ITEM_SLOT_START_RANGE/END_SLOT_END_RANGE`）⇒ 现出一个**缓存物品的副本**，数量＝我们能给的上限（受该物品最大堆叠与缓存存量限制）；
  - `setStackInSlot`：该下标由我们供货 ⇒ 按 **presented − remaining** 算出**实际取走多少**，**扣我们真实的缓存**（走既有的 `CacheEdit.extract` ＋ `CacheLedger.replace` 账本替换路径，并尊重 `CraftingReservations`），**绝不写回他们的容器**；否则交给 `super`；
  - `getSlots`／`isItemValid`／`insertItem`／`extractItem`／`getSlotLimit` **一律委托**；对被借用的下标，`extractItem` 按"取出多少就扣多少"处理且可重复取出直到用完，`insertItem` 在该下标被拒（那时他们的容器不该被我们写）。
- **判定/摆料/显示全在他们的代码里**：我们**不注册 JEI 处理器**、不碰按钮、不加 lang 键、不改他们的注册表。**包装器绝不外泄**：不落任何字段/静态/存档，只活在他们的方法局部里 ⇒ **虚物不可能被序列化或写进他们背包**（他们容器只会因**他们自己**的写回而变）。
- **失败即退化**：没装他们、或他们改了内部形状 ⇒ 三个 mixin 不生效（各留一条 WARN）/ `FxntCompat.present` 直接返回原处理器 ⇒ 行为**与今天完全一致（零影响）**；空位不够 ⇒ **少报几个**（等于退回今天的行为）。
- **一行证据日志**：`FMP compat: exposed=<n> cache stacks to <他们的类名>; served=<m> items`；退化时 `FMP compat: degraded (无空位/无缓存视图/类形状不匹配)`（每类原因一次，不刷屏）。服务端每次真实扣账另打一行 `FMP compat: debited <n> x <物品> from the cache of <玩家>`。

**脆弱性（必须说清）**：这条兼容依赖**他们的内部形状**（类名、方法名、那几个 store 的位置）。他们一改内部就可能失效——届时**安静退化**：不崩、不给假货、也不会扣错账，日志里会看到 `FMP compat: degraded (...)`。
## F-1 更正：注入点类型错了（我们的包装器从未被调用），已按 LVT 改正

**实机证据（用户 05:38–05:40 那一跑）**：两个 mixin **类确实装上了**（`Mixing TransferRecipePacketMixin … into …TransferRecipePacket`、`Mixing JeiCraftingTransferHandlerMixin … into …JEICraftingTransferHandler`），但日志里**没有任何一条 `FMP compat:`**（`exposed=`／`served=`／`degraded` 全无）⇒ 我们的处理器**一次都没跑**。

**根因（LVT 原文，`javap -l`）**：
- 1.3.4 客户端 `JEICraftingTransferHandler.transferRecipe(...)`：`LocalVariableTable` 第 11 号槽 `itemHandler`，签名 `Lnet/neoforged/neoforge/items/IItemHandlerModifiable;`（由 `IBackpackContainer.getItemHandler():ItemStackHandler` 赋值后被**拓宽成接口类型**）；
- 1.3.4 服务端 `TransferRecipePacket.collectAndPlace(Ingredient,int,int,Inventory,IItemHandlerModifiable,List<Slot>)`：`0 303 5 itemHandler L…IItemHandlerModifiable;`（**参数**）；`getMaxCraftableItems(List,Inventory,IItemHandler)`：`0 334 3 backpack L…IItemHandler;`（**参数**）；
- 1.1.2-local 服务端 `ServerPayloadHandler.lambda$handleTransferRecipePacket$9(IPayloadContext, TransferRecipePacket)`：`75 1068 8 itemHandler L…IItemHandlerModifiable;`；`getMaxCraftableItems(...)`：`0 318 3 backpack L…IItemHandler;`。

我们原来把 hook 声明成 `ItemStackHandler`、并打在 `handle` 的局部上 ⇒ **类型不匹配**（`defaultRequire:0` 静默跳过）。**这是上一封定点信息有误，已记入更正。**

**改正**：客户端打在同一处 local 的 **STORE**，声明类型 `IItemHandlerModifiable`；服务端**只打参数**（`@ModifyVariable(at=@At("HEAD"), argsOnly=true)`，**不带 ordinal** —— 该类型在该方法里唯一）：`collectAndPlace` 的 `IItemHandlerModifiable`、`getMaxCraftableItems` 的 `IItemHandler`；1.1.2 另用 `method="*"` + `IItemHandlerModifiable` 的 STORE 命中其 lambda 局部。`handle` 上原先那两个 ordinal 注入**已撤除**。

**自证（本轮硬门槛，已在本机拿到）**：把 1.3.4 的 jar **复制**进临时 dev run 目录（`…\run\gametest\neo21.1.219-mobile-model8\mods\`，**用户实例未动**，跑完即移除我自己放的副本），日志得到：
- `Mixing TransferRecipePacketMixin from fmp_fxntstorage_compat.mixins.json into net.fxnt.fxntstorage.network.packet.TransferRecipePacket`；
- `FMP compat: hooked getMaxCraftableItems (IItemHandler)`、`FMP compat: hooked collectAndPlace (IItemHandlerModifiable)`、`FMP compat: degraded (no cache view: unknown owner)`；
- 该环境 **161/161 通过**（含探针用例）；干净环境同样 **161/161**（探针自报 `skipped`）。

**新增留痕**：每个注入点一次性 INFO `FMP compat: hooked <方法>(<类型>)`；`FxntCompat` 的**每条静默分支**也留痕（`pass-through (no handler)`／`pass-through (their mod is not loaded)`／`degraded (slot layout unknown)`／`degraded (no cache view: unknown owner|nothing available)`）；新增进程内探针用例 `FxntInjectionProbeTests`（反射调用他们的 `getMaxCraftableItems`／`collectAndPlace` 并**断言**我们的 hooked 行已发出；无该模组时自报 skipped 并直接通过）。

**未覆盖（如实）**：客户端那一个注入点**只能在客户端触发**（专用服务端不加载他们的客户端类）⇒ 它的"装上了"由 LVT 定点 + 同一 `@ModifyVariable` 机制 + 用户下次日志里的 `FMP compat: hooked transferRecipe (IItemHandlerModifiable)` 行来证明。

**本轮对象与计数**：jar **382,909 B／`dddd6b46ff7b20300bb000085241479a121477e8b70a42af94744cf292fb4706`**（源码提交 `8965070`）；源码快照 `v0.2.2-source.zip` **0453b71fbc6558fbe3ee7b21078551b271aab385deb30c88d30f8e0f07a450be**；JUnit **37/0/0**（5 个 XML）；GameTest **161/161**（155 ＋ 兼容包装器 5 ＋ 注入探针 1）；成品冒烟 `developmentOutputs=0` + `Done (1.000s)!`；冻结件 `v0.2.1` 未动。
## F-2／F-3 更正：按下标扣账 ＋ 只借"这套配方要用的"格子（两端同一规则）

**实机依据（用户 16:30–16:31）**：① 服务端 `collectAndPlace` 命中并"供货"（66 行、`served=26`）但 **`debited`＝0**，存档 `create_feed_me_packages_ledger.dat` 时间戳仍是 3:46（**本场一个字节没改**）⇒ 扣账在某个 `return` 上**静默早退**，而旧代码**无论扣账成败都 `served++` 并打日志**；② 同一时刻客户端 `exposed=2`、服务端 `exposed=1` ⇒ 旧规则"按空位顺序借任意缓存栈"让**两端借出的集合不一致**（客户端预览够、服务端拿不出）⇒ 按钮与实际不符；③ 客户端**每 tick 一行**刷屏。

**F-2（改为构造上不可能错）**：`CacheSupply.available()` 现在返回 **(cellIndex, stack)** 对；`take(cell, count)` **直接对该格子** `edit.extract` ＋ `ledger.replace`（**不再**用 `ItemVariantKey.of/find` 反查 —— 那正是静默早退的来源），并**返回实际扣掉的数量**；每个"没扣成"的分支都留一行 `FMP compat: debit skipped (<原因>)`；`CachePresentingHandler` 把 **served／debited 分开计**，**只有真正扣掉才计入供货**。

**F-3（两端同一规则）**：借用规则收成**纯函数** `CacheBorrow.plan(空位, 候选格, 材料过滤器)`，**客户端与服务端共用同一实现**：客户端用他们 `transferRecipe` 的**配方材料集**过滤（`RecipeHolder.getIngredients()`），服务端在 `collectAndPlace` 里用**该味 Ingredient**、在 `getMaxCraftableItems` 里用**整份材料表**过滤；**拿不到材料就一格不借**（退回他们原本行为，绝不猜）。

**日志限流**：每次传输**最多一行** —— `FMP compat: borrow exposed=n served=m debited=k from <他们的类名>`（只在真的取走物品时打一次；不再有每 tick 的 `exposed=` 行）。

**本轮硬证据（三条都拿到）**：① **真实账本**：`FxntCacheDebitTests`（GameTest）用**真 `CacheLedger`** 播种两格 → `ServerCacheSupply.take(cell,5)` ⇒ 该格 64→**59**、邻格不动；`take(0,1000)` ⇒ 返回 **59**、该格归 **0**；空格再 `take` ⇒ **0**（并留 `debit skipped`）——**不是只断言日志**；② **按材料过滤**：`CacheBorrowTest.onlyCellsTheRecipeAcceptsAreBorrowed`（纯，断言只有被接受的那一格被借出）＋ `FxntCacheCompatTests.onlyCellsTheRecipeAcceptsArePresented`（进程内包装器，断言未被接受的格子**绝不呈现**）；③ **两端同规则**：`CacheBorrowTest.bothSidesComputeTheSameBorrowSetFromTheSameInput`（同一输入、两份实例 ⇒ 逐槽相同 `cell`/`presented`），且实现上两端**调用同一个函数**。
**注入仍正常（F-1 不回退）**：把他们的 1.3.4 jar 复制进临时 dev run 目录后（用户目录未动、跑完移除），日志得到 `Mixing TransferRecipePacketMixin … into …TransferRecipePacket` ＋ `FMP compat: hooked getMaxCraftableItems (IItemHandler)` ＋ `FMP compat: hooked collectAndPlace (IItemHandlerModifiable)`，该环境 **162/162 通过**。（本轮先出现 `InvalidInjectionException: CallbackInfoReturnable is required!`——取值方法的 HEAD 回调必须用 `CallbackInfoReturnable`——已修正，日志见包内 `pv9-probe-with-fxnt134.log`。）

**计数**：JUnit **38/0/0**（5 个 XML：PanelLayout 8／CacheBorrow 7／JeiIntegrationIsApiOnly 6／CacheState 13／ReturnService 4）；GameTest **162/162**（155 ＋ 包装器 5 ＋ 注入探针 1 ＋ 真实账本扣账 1）；成品冒烟 `developmentOutputs=0` ＋ `Done (0.966s)!`；冻结件 `v0.2.1` 未动。
## F-4 更正：单机里 dist 恒真 ⇒ 服务端那次取货也拿了"客户端视图"，所以永远不扣

**实机依据（用户 16:59 那一跑，摘要行原文）**：`FMP compat: borrow exposed=0 served=3 debited=0 from fxntstorage:TransferRecipePacket#collectAndPlace`（另有 served=1/7/10 多行），且**全日志 `debit skipped`／`pass-through`／`degraded` 各 0 行**，账本时间戳与内容不变。
**根因（我上一轮的设计错）**：`FxntCompat.supplyFor(...)` 用 **`FMLEnvironment.dist.isClient()`** 选边，而**单机（集成服务端）里该值恒为真** ⇒ 服务端线程上那次 `collectAndPlace` 也拿到 `ClientCacheSupply`，它的 `take(...)` 就是 `return 0`（且不打日志）⇒ `debited` 恒 0、账本一字不动、连一条 skip 都看不到。**此前的"注入探针通过"是因为它跑在专用服务端（`dist=SERVER`），没有复现单机形态。**

**修法**（选边按"身份＋线程"，并让选边可见）：
1. **选边抽成纯函数** `SideSelect.choose(distIsClient, onServerThread, matchesServerPlayer)`：`matchesServerPlayer && (onServerThread || !distIsClient)` ⇒ **SERVER_CACHE**，否则 **CLIENT_VIEW**；**匹配到我们的服务器玩家时绝不退回客户端视图**，**也绝不在客户端线程读服务端账本**；身份匹配沿用 `FxntBackpack.handlerOf(player)`，线程判定用 `ServerLifecycleHooks.getCurrentServer().isSameThread()`。
2. **客户端那份 take 大声拒绝**：被调用即打一次 `FMP compat: debit refused (client view cannot write; server side not selected)`。
3. **摘要行改为每次 `account` 打累计并带选边**：`FMP compat: borrow side=<server-cache|client-view> exposed=n served=m debited=k from <他们的类名>`（**served 与 debited 同时给出**；不再"每个 wrapper 只打第一笔"，那正是 `served=3 debited=0` 误导的来源）。每次传输的笔数＝配方材料数，不刷屏。

**本轮门槛证据（§三 两条都拿到）**：
- ① **真值表单测** `SideSelectTest`（5 例）：**`dist=true ＋ serverThread=true ＋ matches=true ⇒ server-cache`**（今天翻车的组合）逐条断言；`matches` 缺失 ⇒ 视图；客户端线程 ⇒ 视图。
- ② **进程内真账本**（GameTest `FxntCacheDebitTests`，服务端线程上走包装器＋真 `CacheLedger`）：日志 `FMP compat: borrow side=server-cache exposed=0 served=64 debited=64 from debit-test`，并断言该格 64→0。
- ③ **记录纪律**：本轮把 `.fmp-build-safe\test-results\test\` 的**本轮新鲜 XML（6 个／43 例）复制**进包目录（旧文件保留，不删）。

**计数**：JUnit **43/0/0**（6 个 XML：PanelLayout 8／CacheBorrow 7／**SideSelect 5**／JeiIntegrationIsApiOnly 6／CacheState 13／ReturnService 4）；GameTest **162/162**；成品冒烟 `developmentOutputs=0` ＋ `Done (1.014s)!`；冻结件 `v0.2.1` 未动。
**仍未覆盖**：专用开发服务端**无法**复现单机（集成服务端＋`dist=CLIENT`），因此"选边"由**真值表单测**＋**摘要里的 `side=` 字段**共同保证；真机仍需用户跑一次确认 `side=server-cache` 且 `debited>0`。
## F-5：扣账成立后的"客户端视图比服务端乐观"——诊断＋一处保守化

**实机依据（用户 17:43 那一跑）**：三行 hooked 俱在；`FMP compat: borrow side=server-cache exposed=0 served=1 debited=1 from fxntstorage:TransferRecipePacket#collectAndPlace` **共 34 行**（`debit refused`／`debit skipped` 各 0）⇒ **选边正确、缓存真被扣**。用户原话：「背包里的铁板全消耗完了，JEI 还说我能合成溜槽，点「+」无事发生，**直到再打开一次背包才刷新**」⇒ **客户端视图比服务端乐观**（按钮亮、服务端拿不出）。

**本轮做了什么**
1. **扣账行点名**：`FMP compat: borrow side=server-cache cell=<i> item=<id> amount=<k> exposed=… served=… debited=… from <他们的类名>`（一行一次）。进程内证据（GameTest 真账本）：`borrow side=server-cache cell=2 item=minecraft:gold_ingot amount=64 exposed=0 served=64 debited=64 from debit-test`。
2. **客户端"三源计数"诊断**（限流：同一内容只打一次）：在他们客户端预览那条路径上逐味材料打
   `FMP compat: preview recipe=<id> | <物品>: inventory=<a> backpack=<b> lent=<c> | …`
   —— `inventory`＝玩家 36 格；`backpack`＝**他们当前佩戴的那个背包栈**（他们的客户端路径用的就是 `BackpackHelper.getEquippedBackpackStack` 取出的佩戴栈，`FxntBackpack.handlerOf` 解析的是同一个，已在源码里核对）；`lent`＝我们借出的缓存量。**下一次日志可直接指出哪一源在骗人。**
3. **保守化修正**：客户端借出的天花板**不再用** `ClientMaterials.craftingStacks()`（它把 `ownReservations` 加回去、必然比服务端乐观），改用与服务端 free 口径一致的 `ClientMaterials.stacks()` ⇒ **两端对同一缓存格给出同一天花板**。

**门槛**：① 纯函数单测 `PreviewCountsTest`（4 例，假 inventory／背包／缓存各一组数字，含"只在缓存里"的情形）；② 天花板一致：`CacheBorrowTest.aReservedAmountIsNeverLentSoBothSidesShareOneCeiling`（预留 10 ⇒ 两端都只借 54）；③ 既有三条门槛（真账本扣账／按材料过滤／选边真值表）**未回退**。
**待批（本轮不擅自加）**：若诊断把矛头指向**他们的客户端背包/玩家背包视图陈旧**（我们改不了他们），可选兜底＝**在他们完成放置后由我们在服务端补一次容器同步**；等诊断结果与用户定夺。
**记录纪律**：已把**已删测试类**（ClientNotice／PreviewPolicy／ProvablyUnsolvable）的旧 XML **移入 `history/`**（移动，未删）；本轮 7 个新鲜 XML 已复制进包目录。

**计数**：JUnit **48/0/0**（7 个 XML：PanelLayout 8／CacheBorrow 8／SideSelect 5／**PreviewCounts 4**／JeiIntegrationIsApiOnly 6／CacheState 13／ReturnService 4）；GameTest **162/162**；成品冒烟 `developmentOutputs=0` ＋ `Done (1.046s)!`；冻结件 `v0.2.1` 未动。
## F-6（核心已落，接线未做）：选边改按"调用点＋原版 Inventory 参数"，不再有任何身份匹配

**实机依据（18:00 那一跑）**：`FMP compat: debit refused (client view cannot write; server side not selected)`（18:00:54.658，**服务端线程**）＋ 同刻 `borrow side=client-view cell=0 item=minecraft:iron_ingot amount=32 served=32 debited=0 from fxntstorage:TransferRecipePacket#collectAndPlace`；本轮 `side=server-cache` **0 行**、`side=client-view` **1 行** ⇒ **服务端那次取货又走了"只展示不扣账"的那一侧**，32 件全没扣。**根因**：F-4 用 `FxntBackpack.handlerOf(player) == original` 的**实例身份**认玩家；这次没匹配上（他们的传输路径用的是新造/另一份容器实例），于是按"未匹配"退回客户端视图、降级成不扣账——**是队长选边逻辑的漏洞**。（那条"响亮拒绝"日志**保留**，它精准抓住了这个 bug。）

**本轮落地（源码提交 `2c`→见下、记录见下）**：新增 `CachePresentingInventory extends net.minecraft.world.entity.player.Inventory`——**他们的服务端方法本来就把玩家真实背包当参数传进来**，于是我们把这个参数换成自己的子类：`this.player`（`Inventory` 的 `public final Player`）**直接就是玩家**，`AccessGate.resolve(player)` **再也不需要任何身份匹配**，也就**不可能"认错人"**。规则与既有窄缝一致：他们格子非空 ⇒ 原样返回；我们借用的空槽 ⇒ 缓存副本（min(该格 free, 堆叠上限)）；`setItem` 写回 ⇒ **按 presented − remaining 精确扣真实缓存、绝不写真实背包**；`removeItem` 同口径；其余委托给玩家真实背包。

**门槛 ①（构造性，已取得）**：GameTest `FxntInventorySeamTests` 用**真玩家＋真账本**驱动这条窄缝：`borrow side=server-cache cell=0 item=minecraft:gold_ingot amount=32 exposed=0 served=32 debited=32 from inventory-seam-test`，并断言 ①`seam.owner() == player`（**owner 就是参数本身，无匹配过程**）②他们的槽原样 ③该格 32→0 ④**真实背包内容一字不变**。

**⚠️ 本轮未做（如实申报，避免误读）**：**mixin 还没换到这条窄缝上**——`collectAndPlace`/`getMaxCraftableItems` 的 `Inventory` 参数注入、客户端 `player.getInventory()` 的只读呈现、以及"删掉 `SideSelect` 与旧的三个 `ItemStackHandler` 注入点"**都还没做**。因此**本包的运行行为仍是 F-5 那一版**（18:00 的身份匹配缝隙**尚未关闭**），F-6 只是把"按构造不可能认错"的那块承重件与它的构造性证据落了下来。下一步（等你下单）：换接线 + 删旧路径 + 端到端门槛（服务端线程上走他们的 `collectAndPlace`，摘要出现 `side=server-cache` 且 `debited>0`）。

**计数**：JUnit **48/0/0**（7 个 XML）；GameTest **163/163**（162 ＋ 新增窄缝构造性用例 1）；成品冒烟 `developmentOutputs=0` ＋ `Done (1.284s)!`；冻结件 `v0.2.1` 未动。