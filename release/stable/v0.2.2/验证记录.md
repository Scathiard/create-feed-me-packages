# 验证记录（0.2.2）

对象：`release/stable/v0.2.2/create-feed-me-packages-0.2.2.jar` ＝ **369,575 B／`f51c28510ddf0d83186ffddfa39b4ce46d8950b8d239b469dd4e240d692cf2ad`**（分支 `release/0.2.2` @ **`9c39bc8`**，构建于 worktree `D:\claude_sandbox\fmp-021-patch`，共享产物目录 `D:\claude_sandbox\.fmp-build-safe`）。

> 本对象包含"三处料源（合成格＋背包＋缓存）"补丁；此前 369,427 B／`5205671649…` 的成品已被本次构建覆盖（同一路径固定输出），旧值仅作历史记号。

## 一、三条合同命令（原始计数）

1. `.\gradlew.bat test build --offline --no-daemon --no-configuration-cache --console=plain` ⇒ **BUILD SUCCESSFUL**；JUnit **tests=25／failures=0／errors=0／skipped=0**（3 个 XML：PanelLayoutTest 8／CacheStateTest 13／ReturnServiceTest 4）。
2. `.\gradlew.bat runGameTestServer -PfmpGameTests -PwithMobile --offline --no-daemon --no-configuration-cache --console=plain` ⇒ **BUILD SUCCESSFUL**；`========= 155 GAME TESTS COMPLETE IN 15.77 s` ＋ **`All 155 required tests passed :)`**。（**必须带 `-PwithMobile`**：少该档位会落到不含 mobile 的 run profile，套件不同，会伪装成上百条失败——本轮之前已实测到这个坑。）
3. `.\gradlew.bat runServer -PfmpPackaged -PwithMobile --offline --no-daemon --no-configuration-cache --console=plain` ⇒ 构建侧自证 **`FMP_PACKAGED_SOURCE jar=D:\claude_sandbox\.fmp-build-safe\libs\create-feed-me-packages-0.2.2.jar developmentOutputs=0`**；服务端日志 Mod 列表 **`Create: Feed Me Packages! 0.2.2`**、`Loading Create: Feed Me Packages! 0.2.2`、**`Done (1.235s)!`**。该成品与服务端启动后 `runServer` 在本地**不会自行退出**（进程空转），本轮在拿到上述启动标记后**主动终止**了该次 gradle 调用；`BUILD SUCCESSFUL` 未打印，**如实记为"启动成功、未自然收尾"**。

## 二、静态自证（发布线干净）

- jar 条目共 **254**；接管相关**有且仅有** `dev/scathiard/feedmepackages/compat/jei/takeover/TakeoverRecipeTransferManagerMixin.class` ＋ `fmp_jei_takeover.mixins.json`。
- jar 内**没有** `compat/jei/takeover/TakeoverGate*`、**没有** `jei_diag.mixins.json`、**没有** `dev/scathiard/feedmepackages/diag/**`；唯一命中 `Diag` 的是**既有的正式混入** `dev/scathiard/feedmepackages/mixin/ClientContentDiagnosticMixin.class`（非本次带入）。
- 包内 `META-INF/neoforge.mods.toml`：`version="0.2.2"`，两条 `[[mixins]]`＝`create_feed_me_packages.mixins.json`、`fmp_jei_takeover.mixins.json`；包内 `fmp_jei_takeover.mixins.json` ＝ `required:false`／`minVersion 0.8`／`compatibilityLevel JAVA_21`／`"client": ["TakeoverRecipeTransferManagerMixin"]`／`injectors.defaultRequire 0`（**无 gate、无 server 段**）。
- **本次修复的改动面**（`git diff 46eaf05..9c39bc8`，3 个文件 **+13/−4**）：`compat/jei/FmpRecipeTransfer.java`（`transferRecipe` 预览分支新增"原生预览成功即返回 null"；`performTransfer` 改为先 `nativeTransfer(..., perform=true)` 再 `fillRecipe`，原生已成功则直接返回 null）＋ `lang/zh_cn.json`／`lang/en_us.json`（`result.missing_material` 文案写明三处料源）。**未带 A1**（`3×3 去 MenuType`）：接管判定仍是 `menu.getClass() != CraftingMenu.class` 精确类 ＋ `RecipeTypes.CRAFTING` 引用相等。
- **注入点签名复核（javap 实跑）**：`mezz.jei.library.recipes.RecipeTransferManager#getRecipeTransferHandler` 在 `jei-1.21.1-neoforge-19.21.0.246` 与 `19.51.0.418`（本仓 `jei_version=19.51.0.418`，编译期 `compileOnly` 用它）均为 `<C extends AbstractContainerMenu, R> Optional<IRecipeTransferHandler<C,R>> getRecipeTransferHandler(C, IRecipeCategory<R>)`；混入声明 `private void fmp$takeover(AbstractContainerMenu, IRecipeCategory<?>, CallbackInfoReturnable<Optional<?>>)` ＋ `at=HEAD` ＋ `cancellable=true`，**擦除后参数类型一致**。（这是"签名对不对"的静态证据，**不等于**混入已在客户端生效。）
- 源文件无 BOM（IDE 会因 BOM 编译失败），三个 JSON（`fmp_jei_takeover.mixins.json`、`zh_cn.json`、`en_us.json`）以 UTF-8 读出后均可解析（105 键）。
- 源码快照 `v0.2.2-source.zip` ＝ **438,305 B／`d862f5ad50201e37c4240ae6a6bd34e48d84a13baca7e4487b15b8c9d2dee844`**（`git archive release/0.2.2 @ 9c39bc8`，299 条目）；与本目录成品 hash 一并写在 `SHA256SUMS.txt`／`SOURCE_SHA256SUMS.txt`。
- 已发布冻结件复核未变：`release/stable/v0.2.1/create-feed-me-packages-0.2.1.jar` ＝ **366,915 B／`93622b3f4125ebffe2018c287606994519784f46ae9016c0aae203088d708cf7`**（`fmp-021-patch/release/v0.2.1/` 副本同值）。`release/stable/v0.2.1/` 未被本次改动触碰。

## 三、未覆盖（如实）

- **客户端两态实机验收未做**（本机无客户端环境）：见 `测试清单.md`，由用户实机执行。**混入是否真正生效、三处料源在真实界面上是否如预期，只能在客户端证明**；本节的服务端证据只能证明"jar 能加载、混入配置不炸、注入点签名对得上"。
- 专用服务端启动日志里**没有** `fmp_jei_takeover.mixins.json` 的运行期打印（该配置只有 `client` 段；服务端 Env=SERVER 下不应用），因此**不能**用本轮日志作为"混入已应用"的证据。
- 本轮观察到的既有噪音：`Error loading class: org/jetbrains/annotations/ApiStatus$ScheduledForRemoval`（某模组注解类，与本模组无关）、`FMP cache ledger locked; original data retained`（存档锁定负路径用例的**预期** ERROR 日志）、收尾阶段 `java.io.IOException: 另一个程序已锁定文件的一部分`（日志文件被占用）。三者都**未影响**上述计数与启动标记，如实记录。
