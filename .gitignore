.gradle/
build/
run/
out/
bin/
.idea/
.vscode/
*.iml
*.ipr
*.iws
eclipse/
src/generated/resources/.cache/

!gradle/wrapper/gradle-wrapper.jar

# Generated / local runtime
logs/

# Build artifacts (regenerable) — keep the release/*.md records, ignore the jars
release/**/*.jar
# Snapshot archives and one-off release tooling/logs stay in the working tree, not in git
release/**/*.zip
release/**/make-snapshot.py
release/**/smoke_packaged_client.py
*.mp4
*.out
compile-out.txt
full-out.txt
gt-out.txt
release/v*-test.*/
release/testing/

# The release/ folder stays on this machine only. Published artifacts live on GitHub Releases,
# and the historical evidence stays in the git history. Untracked on purpose (2026-09-17).
release/

# Internal project documents / collaboration rules (not for the public repo)
docs/
AGENTS.md
doc示例/
docs.zip
迁移对照.md

# Verification / generation tooling (not needed to build the mod)
tools/

# Archived legacy & reference implementations (not part of the repo history)
legacy-root-test12/
legacy-root-test17-src/
reference/

# Dev screenshots / screen recordings
参考/
测试录屏/

# Editor cache
.obsidian/

# Legacy one-off migration / patch scripts
patch_*.py
list_errors.py

